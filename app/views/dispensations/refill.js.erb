document.getElementById("modalContent").innerHTML = "<%= j (render partial: 'refill') %>"
document.getElementById("appModal").style.display = "block"
document.getElementById("appModal").classList.add("show")

$(function() {
    
    $('#provider').typeahead({
        ajax: { 
            url: '/provider_suggestions',
            triggerLength: 2 
            }
    });
});
